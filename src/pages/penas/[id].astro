---
import Layout from '../../layouts/Layout.astro';
import Navigation from '../../components/Navigation.astro';
import Footer from '../../components/Footer.astro';
import penas from '../../data/penas.json';

export function getStaticPaths() {
  return penas.map(pena => ({
    params: { id: pena.id },
    props: { pena }
  }));
}

const { pena } = Astro.props;
---

<Layout title={`${pena.nombre} - Peñas Flamencas`} description={pena.descripcion}>
  <Navigation />
  
  <section class="detail-header">
    <div class="container">
      <a href="/penas" class="back-link">← Volver a Peñas</a>
      <div class="header-content">
        <div class="header-icon">🎸</div>
        <div>
          <h1 class="detail-title">{pena.nombre}</h1>
          <p class="detail-subtitle">{pena.provincia}</p>
        </div>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <div class="detail-grid">
        <div class="detail-main">
          <div class="detail-card">
            <h2 class="section-heading">Sobre la Peña</h2>
            <p class="detail-description">{pena.descripcion}</p>
          </div>
          
          <div class="detail-card">
            <h2 class="section-heading">Información de Contacto</h2>
            <div class="contact-info">
              <div class="contact-item">
                <div class="contact-item-header">
                  <span class="contact-icon">📍</span>
                  <strong>Dirección</strong>
                </div>
                <p>{pena.direccion}</p>
              </div>

              {pena.telefono && (
                <div class="contact-item">
                  <div class="contact-item-header">
                    <span class="contact-icon">📞</span>
                    <strong>Teléfono</strong>
                  </div>
                  <p><a href={`tel:${pena.telefono}`}>{pena.telefono}</a></p>
                </div>
              )}

              {pena.email && (
                <div class="contact-item">
                  <div class="contact-item-header">
                    <span class="contact-icon">✉️</span>
                    <strong>Email</strong>
                  </div>
                  <p><a href={`mailto:${pena.email}`}>{pena.email}</a></p>
                </div>
              )}
            </div>
          </div>
        </div>
        
        <aside class="detail-sidebar">
          <div class="sidebar-card">
            <h3>Provincia</h3>
            <p class="highlight-text">{pena.provincia}</p>
          </div>
          
          <div class="sidebar-card">
            <h3>Visita la Peña</h3>
            <p class="sidebar-text">
              Las peñas flamencas son espacios abiertos a todos los amantes del flamenco. 
              Consulta sus horarios y programación para disfrutar de veladas flamencas auténticas.
            </p>
          </div>
          
          <div class="sidebar-card cta-card">
            <h3>¿Conoces esta peña?</h3>
            <p class="sidebar-text">
              Comparte tu experiencia y ayuda a otros aficionados al flamenco a descubrir estos espacios únicos.
            </p>
          </div>
        </aside>
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  .detail-header {
    background: rgba(230, 245, 240, 0.95) !important;
    backdrop-filter: blur(10px);
    padding: var(--spacing-xl) 0 var(--spacing-3xl);
    margin-bottom: var(--spacing-xl);
  }

  .back-link {
    display: inline-flex;
    align-items: center;
    color: var(--color-primary);
    text-decoration: none;
    margin-bottom: var(--spacing-lg);
    font-weight: 600;
    transition: color var(--transition-fast);
  }

  .back-link:hover {
    color: var(--color-primary-dark);
  }
  
  .header-content {
    display: flex;
    align-items: center;
    gap: var(--spacing-lg);
  }
  
  .header-icon {
    font-size: 5rem;
    background: rgba(10, 10, 10, 0.3);
    padding: var(--spacing-lg);
    border-radius: var(--radius-lg);
  }
  
  .detail-title {
    font-size: clamp(2rem, 4vw, 3rem);
    margin-bottom: var(--spacing-xs);
    color: var(--color-primary);
  }

  .detail-subtitle {
    font-size: 1.25rem;
    color: var(--color-primary);
    font-weight: 600;
  }
  
  .detail-grid {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: var(--spacing-xl);
  }
  
  .detail-main {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-lg);
  }
  
  .detail-card {
    background: var(--color-bg-medium);
    padding: var(--spacing-xl);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
  }
  
  .section-heading {
    color: var(--color-primary);
    margin-bottom: var(--spacing-md);
    font-size: 1.75rem;
  }
  
  .detail-description {
    font-size: 1.1rem;
    line-height: 1.8;
    color: var(--color-text-secondary);
  }
  
  .contact-info {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-lg);
  }
  
  .contact-item {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xs);
  }

  .contact-item-header {
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
  }

  .contact-icon {
    font-size: 1.5rem;
    flex-shrink: 0;
  }

  .contact-item strong {
    color: #000000;
    font-size: 1rem;
  }

  .contact-item p {
    color: #000000;
    margin: 0;
    overflow: hidden;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .contact-item a {
    color: var(--color-primary);
    text-decoration: none;
    transition: color var(--transition-fast);
    display: block;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .contact-item a:hover {
    color: var(--color-primary-dark);
    text-decoration: underline;
  }
  
  .detail-sidebar {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-lg);
  }
  
  .sidebar-card {
    background: var(--color-bg-medium);
    padding: var(--spacing-lg);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
  }
  
  .sidebar-card h3 {
    color: var(--color-primary);
    margin-bottom: var(--spacing-sm);
    font-size: 1.25rem;
  }
  
  .highlight-text {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-text-primary);
    font-family: var(--font-heading);
  }
  
  .sidebar-text {
    color: var(--color-text-secondary);
    line-height: 1.6;
  }
  
  .cta-card {
    background: var(--gradient-primary);
    color: #000000;
  }

  .cta-card h3,
  .cta-card .sidebar-text {
    color: #000000;
  }
  
  @media (max-width: 968px) {
    .detail-header {
      padding: var(--spacing-lg) 0 var(--spacing-xl);
    }

    .header-icon {
      font-size: 3.5rem;
      padding: var(--spacing-md);
    }

    .detail-title {
      font-size: 1.75rem;
    }

    .detail-subtitle {
      font-size: 1rem;
    }

    .detail-grid {
      grid-template-columns: 1fr;
      gap: var(--spacing-lg);
    }

    .header-content {
      flex-direction: column;
      text-align: center;
    }

    .detail-card,
    .sidebar-card {
      padding: var(--spacing-lg);
    }

    .section-heading {
      font-size: 1.5rem;
    }

    .contact-icon {
      font-size: 1.5rem;
    }

    .cta-card h3 {
      font-size: 1.1rem;
    }

    .cta-card .sidebar-text {
      font-size: 0.9rem;
      line-height: 1.5;
    }
  }
</style>
